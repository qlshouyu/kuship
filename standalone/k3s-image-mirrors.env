# k3s 系统镜像的「镜像源覆盖表」（单一真相，供 images-package.sh 使用）。
#
# 背景：
#   images-package.sh 默认按 k3s 官方 k3s-images.txt 从 docker.io 直连拉取系统镜像。
#   国内网络下 docker.io 常不稳/超时，导致离线包中个别镜像的层不完整，集群启动时
#   kubelet 退化为在线拉取并失败（典型：metrics-server 一直 ImagePullBackOff）。
#
# 机制：
#   每行 "<k3s-images.txt 中的原始镜像>  <可达的镜像源>"（空白分隔，可对齐）。
#   打包时改为从「镜像源」拉取，再 `docker tag` 回「原始镜像」名字后 save。
#   关键：离线包内必须是「原始镜像」名字，否则 kubelet 仍按原始名去 docker.io 拉，
#         改源拉取等于白做（k3s 的 deployment 引用的是原始 docker.io/rancher/... 名）。
#
# 维护：
#   升级 k3s（standalone/k3s-version.env 的 K3S_VERSION）后，若被覆盖镜像的 tag 变了，
#   需同步更新本表两侧的 tag（原始侧对照新版 k3s-images.txt，镜像源侧对照 aliyun）。
#
# 留空（删除/注释所有行）即关闭覆盖，images-package.sh 退化为全部直连 docker.io。
K3S_IMAGE_MIRRORS="
docker.io/rancher/mirrored-metrics-server:v0.8.1  registry.cn-hangzhou.aliyuncs.com/google_containers/metrics-server:v0.8.1
"
